# Temporary PWA icons

These solid dark-teal icons with a white `B` are temporary technical-test assets derived from the dashboard's existing BUX mark and colors.

Replace all PNG files in this directory with approved final BUX brand artwork before public launch. Preserve the current filenames and dimensions so the manifest and Apple touch-icon link remain valid.
